import AdventureLanding from "../components/AdventureLanding";

/** Landing longue (toutes les sections) — l’entrée bibliothèque est sur `/`. */
export default function LandingPage() {
  return <AdventureLanding />;
}
