import HeroGalleryInner from "../components/HeroGalleryInner";

/** Expérience visuelle — route dédiée (import statique, pas de dynamic). */
export default function ExperiencePage() {
  return <HeroGalleryInner />;
}
